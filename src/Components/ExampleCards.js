
import React from "react";
import { css } from '@emotion/core';


//Going to add hover effect to a vertical rectangle with no borders on image top half summary & desc bottom. Cant tonight. too tired. 😴

export default function ProjectCard( props ) {
    const { children,... rest} = props
    return (
        <div css={ style }>
            
        </div>
    )
 }